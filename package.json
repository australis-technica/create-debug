{
  "name": "create-debug-project",
  "version": "1.0.1-0",
  "private": true,
  "description": "create debug from \"debug\" using module.id   redirects to stdout if env.DEBUG_TO === \"stdout\"    ...WIP",
  "main": "index.js",
  "scripts": {
    "build": "lerna --no-sort run build",
    "clean-modules": "rimraf **/node_modules/* && rm yarn.lock && yarn",
    "test": "echo doesn\\'t work && lerna --no-sort run test",    
    "set-version": "node ./scripts/set-version.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/australis-technica/create-debug.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/australis-technica/create-debug/issues"
  },
  "homepage": "https://github.com/australis-technica/create-debug#readme",
  "devDependencies": {
    "lerna": "^2.11.0"
  },
  "workspaces": [
    "packages/create-debug",
    "packages/module-x",
    "packages/app-x"
  ]
}
